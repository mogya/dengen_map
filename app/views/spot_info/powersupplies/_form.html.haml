= form_for @spot_info_powersupply do |f|
  - if @spot_info_powersupply.errors.any?
    #error_explanation
      %h2= "#{pluralize(@spot_info_powersupply.errors.count, "error")} prohibited this spot_info_powersupply from being saved:"
      %ul
        - @spot_info_powersupply.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
