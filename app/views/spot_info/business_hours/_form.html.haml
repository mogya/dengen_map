= form_for @spot_info_business_hour do |f|
  - if @spot_info_business_hour.errors.any?
    #error_explanation
      %h2= "#{pluralize(@spot_info_business_hour.errors.count, "error")} prohibited this spot_info_business_hour from being saved:"
      %ul
        - @spot_info_business_hour.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
